[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pack-companions-sdk"
version = "0.2.1"
description = "Python SDK for the Pack Companions service — AI companions across the Pack ecosystem."
readme = "README.md"
requires-python = ">=3.10,<4"
authors = [
    {name = "GetPacked"}
]
license = "MIT"
keywords = ["pack-companions", "ai", "companions", "getpacked"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.27.0,<1.0",
    "pydantic>=2.0.0,<3.0",
    "tzdata>=2024.1",
]

[project.urls]
Homepage = "https://github.com/pack-companions/pack-companions-sdk"
Repository = "https://github.com/pack-companions/pack-companions-sdk"
Issues = "https://github.com/pack-companions/pack-companions-sdk/issues"
"Service repo" = "https://github.com/pack-companions/pack-companions"

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3,<10.0",
    "pytest-asyncio>=0.23.0,<2.0",
    "ruff>=0.5.0,<1.0",
    "mypy>=1.10.0,<2.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["pack_companions*"]

[tool.setuptools.package-data]
pack_companions = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
